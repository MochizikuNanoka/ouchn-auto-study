# v2.0.12 发布说明

## 修复任务定位漂移

- 不再把全局 DOM 索引作为任务身份；实际导航通过章节名、完整标题、视频/考试类型与章节内课程项序号重新定位。
- 修复页面实际标题为 `.title` 时被旧选择器读空的问题，完整保留 `（1）`、`（2）` 等区分序号。
- 返回课程页后若 DOM 顺序变化，脚本会记录新旧索引并点击重新定位到的正确任务。

## 断点保护

- 断点新增章节内课程项序号。
- 旧断点的标题与当前任务不一致时，拒绝按旧节次序号误续跑。

## 验证

- 覆盖索引漂移重定位与断点标题不一致保护的回归测试已通过。
